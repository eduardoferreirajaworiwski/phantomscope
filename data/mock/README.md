# Mock Data

Offline mode uses deterministic mock enrichment so the project can be demonstrated without live network access.

Use this mode for:

- interviews
- demos
- local development without provider credentials
- resilience testing when external APIs fail

